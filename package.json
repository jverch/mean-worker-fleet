{
  "name": "mean-worker-fleet",
  "version": "1.0.0",
  "description": "Uses python workers to calculate an average across many files.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "jverch",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
